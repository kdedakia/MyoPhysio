#include "move.h"

Move::Move(double yaw, double pitch, double roll, double aX, double aY, double aZ,
		double wX, double wY, double wZ) {
	yaw = yaw;
	this.pitch = pitch;
	this.roll = roll;
	this.aX = aX;
}